// src/types/User.d.ts
export interface User {
  id: string; // UUID
  firstName: string;
  lastName: string;
  email: string;
  // Add any other properties that are part of your user object
}
